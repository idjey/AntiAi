{
    "name": "@antiai/database",
    "version": "0.1.0",
    "private": true,
    "description": "Prisma database schema for AntiAI.me",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "generate": "npx prisma generate",
        "push": "npx prisma db push",
        "migrate": "npx prisma migrate dev",
        "migrate:deploy": "npx prisma migrate deploy",
        "studio": "npx prisma studio",
        "build": "tsc",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "prisma": "^5.22.0",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "@prisma/client": "^5.22.0",
        "bcrypt": "^5.1.1"
    }
}