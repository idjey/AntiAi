// Vercel Serverless Function entry point for NestJS
// This file must live in the `api/` directory at the project root (apps/api/api/).
// Vercel auto-detects files in `api/` as serverless functions.
import handler from '../src/main';

export default handler;
